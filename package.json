{
  "name": "messenger",
  "version": "1.0.0",
  "description": "messenger application ",
  "license": "ISC",
  "author": "",
  "private": true,
  "type": "module",
  "main": "index.js",
  "scripts": {
    "setup": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n \"S,C\" -c \"yellow,cyan\" \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && nodemon index.js",
    "dev:client": "cd client && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "concurrently": "^9.2.1",
    "dotenv": "^17.0.0",
    "motion": "^12.23.24",
    "nodemon": "^3.1.10"
  }
}
