# Messenger App

A full-stack messaging app with React + Express + MySQL.

## 🧩 Setup

1. **Clone the repo:**
   ```bash
   git clone https://github.com/anveshdandala/messenger.git
   cd messenger
   ```
2. **Install all dependencies :**
   ```bash
   npm run setup
   ```
3. **Add environment variables:**

4. **Start both servers:**

```bash
npm run dev

```
